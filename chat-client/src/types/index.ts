export type User = {
    fullname: string;
    username: string;
    email: string;
    password: string;
    confirmPassword: string;
};
export type UserCredentials = {
    email: string,
    password: string,
}

